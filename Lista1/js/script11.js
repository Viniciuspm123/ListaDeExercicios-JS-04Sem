var raio = parseFloat(prompt("Digite o raio do seu circulo:"))

var area = Math.PI * (raio * raio )

alert (" O Area do seu circulo é " + area.toFixed(2)); //"toFixed(2))" usado para limitar as casas decimais.